<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Facture Commande #{{ order.reference }}</title>
    <style>
        body { font-family: DejaVu Sans, sans-serif; font-size: 12px; } /* Use DejaVu Sans for better character support */
        h1, h2, h3, h4, h5, h6 { margin: 0 0 10px 0; padding: 0; }
        p { margin: 0 0 5px 0; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ccc; padding: 8px; text-align: left; }
        th { background-color: #eee; }
        .container { width: 100%; margin: 0 auto; padding: 20px; }
        .header { margin-bottom: 30px; overflow: auto; }
        .header .logo { float: left; width: 150px; } /* Adjust size as needed */
        .header .company-info { float: right; text-align: right; }
        .addresses { margin-bottom: 30px; overflow: auto; }
        .addresses .shipping-address { float: left; width: 48%; }
        .addresses .billing-address { float: right; width: 48%; }
        .items-table th, .items-table td { text-align: right; }
        .items-table th:first-child, .items-table td:first-child { text-align: left; }
        .total-row th { text-align: right; }
        .total-row th:first-child { text-align: left; }
        .footer { margin-top: 50px; text-align: center; font-size: 10px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
           
            <div class="company-info">
                <h3>Facture</h3>
                <p>N° Facture : {{ order.reference }}</p>
                <p>Date : {{ order.createdAt|date('d/m/Y') }}</p>
                <p>Statut Paiement : {{ order.paymentStatusLabel }}</p> {# Use the helper method from Order entity #}
                {% if order.paidAt %}
                    <p>Date Paiement : {{ order.paidAt|date('d/m/Y H:i') }}</p>
                {% endif %}
            </div>
        </div>

        <div class="addresses">
            <div class="shipping-address">
                <h4>Adresse de Livraison</h4>
                <p>{{ order.shippingAddress|nl2br }}</p>
            </div>
           
        </div>

        <table class="items-table">
            <thead>
                <tr>
                    <th>Produit</th>
                    <th>Prix Unitaire</th>
                    <th>Quantité</th>
                    <th>Total Ligne</th>
                </tr>
            </thead>
            <tbody>
                {% for item in order.orderItems %}
                    <tr>
                        <td>
                            {{ item.productName ?? 'Produit' }}
                            {% if item.product and item.product.id %}
                                <br><span style="font-size: 10px; color: #555;">Réf: {{ item.product.id}}</span>
                            {% endif %}
                        </td>
                        <td>{{ item.price }} TND</td> {# Adjust currency filter #}
                        <td>{{ item.quantity }}</td>
                        <td>{{ (item.price * item.quantity)}} TND</td> {# Adjust currency filter #}
                    </tr>
                {% endfor %}
            </tbody>
            <tfoot>
                 <tr class="total-row">
                     <th colspan="3" style="text-align: right;">Sous-total</th>
                     <th>{{ order.totalAmount}} TND</th> {# Adjust currency filter #}
                 </tr>
                 <tr class="total-row">
                     <th colspan="3" style="text-align: right;">Frais de Livraison</th>
                     <th>Gratuit</th> {# Replace with actual shipping cost #}
                 </tr>
                 <tr class="total-row">
                     <th colspan="3" style="text-align: right;">Total</th>
                     <th>{{ order.totalAmount }} TND</th> {# Adjust currency filter #}
                 </tr>
            </tfoot>
        </table>

        <div class="footer">
            <p>&copy; {{ "now"|date("Y") }} Votre Entreprise. Tous droits réservés.</p>
            <p>Tunis, 6180  | 24308496 | hadil.hammami@isimg.tn</p>
        </div>
    </div>
</body>
</html>
