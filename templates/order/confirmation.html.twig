{% extends 'base.html.twig' %}

{% block title %}Confirmation de commande{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-success">
                <div class="card-header bg-success text-white">
                    <h2 class="mb-0"><i class="fas fa-check-circle mr-2"></i>Confirmation de commande</h2>
                </div>
                <div class="card-body">
                    <div class="text-center mb-4">
                        <div class="display-4 text-success mb-3">
                            <i class="fas fa-check-circle fa-2x"></i>
                        </div>
                        <h3>Merci pour votre commande !</h3>
                        <p class="lead">Votre commande #{{ order.reference }} a été confirmée avec succès.</p>
                    </div>
                    
                    <div class="alert alert-info">
                        <p class="mb-0">
                            <i class="fas fa-info-circle mr-2"></i>Un email de confirmation a été envoyé à <strong>{{ order.user.email }}</strong>
                        </p>
                    </div>
                    
                    <div class="card mb-4">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">Détails de la commande</h5>
                        </div>
                        <div class="card-body">
                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <p class="mb-1"><strong>Numéro de commande :</strong> #{{ order.reference }}</p>
                                    <p class="mb-1"><strong>Date :</strong> {{ order.createdAt|date('d/m/Y H:i') }}</p>
                                    <p class="mb-1"><strong>Statut :</strong> 
                                        <span class="badge {% if order.status == 'PAID' %}bg-success{% else %}bg-warning{% endif %}">
                                            {% if order.status == 'PAID' %}Payée{% else %}En attente de paiement{% endif %}
                                        </span>
                                    </p>
                                    <p class="mb-0"><strong>Méthode de paiement :</strong> 
                                        {% if order.paymentMethod == 'credit_card' %}
                                            Carte de crédit
                                        {% elseif order.paymentMethod == 'paypal' %}
                                            PayPal
                                        {% elseif order.paymentMethod == 'bank_transfer' %}
                                            Virement bancaire
                                        {% endif %}
                                    </p>
                                </div>
                                <div class="col-md-6">
                                    <h6>Adresse de livraison :</h6>
                                    <address>{{ order.shippingAddress|nl2br }}</address>
                                </div>
                            </div>
                            
                            <h6 class="mb-3">Articles commandés :</h6>
                            <div class="table-responsive">
                                <table class="table table-bordered table-striped">
                                    <thead>
                                        <tr>
                                            <th>Produit</th>
                                            <th class="text-center">Prix</th>
                                            <th class="text-center">Quantité</th>
                                            <th class="text-center">Total</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {% for item in order.orderItems %}
                                            <tr>
                                                <td>{{ item.product.name }}</td>
                                                <td class="text-center">{{ item.price|format_currency('EUR') }}</td>
                                                <td class="text-center">{{ item.quantity }}</td>
                                                <td class="text-center">{{ item.subtotal|format_currency('EUR') }}</td>
                                            </tr>
                                        {% endfor %}
                                    </tbody>
                                    <tfoot>
                                        <tr>
                                            <th colspan="3" class="text-end">Total</th>
                                            <th class="text-center">{{ order.totalAmount|format_currency('EUR') }}</th>
                                        </tr>
                                    </tfoot>
                                </table>
                            </div>
                        </div>
                    </div>
                    
                    <div class="alert alert-warning">
                        <h6 class="alert-heading">Instructions de livraison :</h6>
                        <p class="mb-0">Votre commande sera préparée et expédiée dans un délai de 2 à 3 jours ouvrés. Un email de suivi vous sera envoyé dès que votre colis sera expédié.</p>
                    </div>
                    
                    <div class="d-flex justify-content-center mt-4">
                        <a href="{{ path('app_order_index') }}" class="btn btn-primary me-3">
                            <i class="fas fa-list mr-2"></i>Mes commandes
                        </a>
                        <a href="{{ path('app_product_index') }}" class="btn btn-outline-secondary">
                            <i class="fas fa-shopping-cart mr-2"></i>Continuer mes achats
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}